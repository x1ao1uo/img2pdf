# 代码审计验收报告 — img2pdf

| 项 | 内容 |
|---|---|
| 项目路径 | `img2pdf/` |
| 审计日期 | 2026-07-19 |
| 审计角色 | 甲方代码验收审计（只读，不改业务代码） |
| 技术栈 | Rust CLI，printpdf + image |
| 总评 | **有条件通过** |

---

## 1. 审计范围与限制

**范围：**

- `src/{main,images,pdf}.rs`
- `Cargo.toml`、`Cargo.lock`、`quality-gate.sh`、`LICENSE`、`README.md`

**限制：**

- 未用真实图片集跑端到端 PDF 目视验收。
- 未执行 quality-gate。

---

## 2. 问题清单

### I-01 业务文档缺失

| 字段 | 内容 |
|---|---|
| 级别 | **一般** |
| 描述 | README 仅为质量门禁；`Cargo.toml` description 为 “Fixed A4 2x2 image grid PDF batch tool”，但无目录约定、支持格式、失败行为说明。 |
| 证据 | `README.md`；`src/main.rs`（位置参数 input/output） |
| 影响 | 甲方功能验收需靠读源码推断。 |
| 验收建议 | **有条件接受**：补 README（输入目录规则、2x2 布局、输出命名）。 |

### I-02 CLI 简陋、无显式帮助

| 字段 | 内容 |
|---|---|
| 级别 | **一般** |
| 描述 | 使用 `env::args` 位置参数，无 clap/`--help`、无版本信息。 |
| 证据 | `src/main.rs` |
| 影响 | 误用成本高，可验收性下降。 |
| 验收建议 | **有条件接受**。 |

### I-03 错误处理粒度

| 字段 | 内容 |
|---|---|
| 级别 | **建议** |
| 描述 | 单组失败打印后继续，进程仍返回 Ok；无统一退出码区分「全部失败」。生产路径未见滥用 unwrap（unwrap 主要在测试）。 |
| 证据 | `src/main.rs` 循环；`src/images.rs` / `src/pdf.rs` 测试块 |
| 影响 | 自动化脚本难以判断整体成败。 |
| 验收建议 | **接受**。 |

### I-04 路径/资源边界

| 字段 | 内容 |
|---|---|
| 级别 | **建议** |
| 描述 | 工具本地批处理，**未发现**密钥；大图/海量文件场景无内存与磁盘上限说明，证据不足判定是否 OOM。 |
| 证据 | `src/pdf.rs`；`src/images.rs` |
| 影响 | 极端输入下稳定性未知。 |
| 验收建议 | **接受**（按工具类风险接受）。 |

### 正面观察

| 字段 | 内容 |
|---|---|
| 级别 | 信息 |
| 描述 | 有分组发现与 PDF 写出单元测试（含 lopdf 校验）；存在 `quality-gate.sh` 与 `Cargo.lock`；`LICENSE` 齐全。 |

---

## 3. 维度摘要

| 维度 | 结论 | 说明 |
|---|---|---|
| 功能可验收性 | 中等 | 逻辑简单可测，缺文档与帮助 |
| 安全/密钥 | 好 | 无网络/密钥面 |
| 可靠性 | 中等 | 部分失败可继续；退出码弱 |
| 可维护性 | 较好 | 模块少而清晰 |
| 测试/门禁 | 较好 | 单元测试 + gate |
| 依赖/构建 | 较好 | |
| 运维配置 | N/A | 本地工具 |
| 文档交接 | 不足 | |

---

## 4. 总评

**有条件通过。**

工具类交付风险可控；补齐 README/`--help` 即可达常规验收。无致命/严重安全缺陷（在当前静态审查证据下）。

**严重问题数：0**  
**致命问题数：0**
