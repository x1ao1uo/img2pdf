# Project Verify Command

Use the local `project-verify` skill for this repository.

Steps:
1. Read `CLAUDE.md` for repository-specific verification commands.
2. Invoke `/project-verify` or follow `.claude/skills/project-verify/SKILL.md`.
3. If only Claude configuration changed, validate `.claude/settings.json` and `.mcp.json` JSON instead of running expensive build/test commands.
4. Report the exact commands run and whether they passed.
